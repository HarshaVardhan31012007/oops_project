{
  "name": "travel-tour-management",
  "version": "1.0.0",
  "description": "A comprehensive travel and tour management system",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["travel", "tour", "management", "booking", "react", "nodejs"],
  "author": "Travel Tour System",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
